{% extends 'base.html.twig' %}

{% block title %}Bubble{% endblock %}

{% block body %}
    <h1>Bubble</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bubble.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bubble.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_bubble_index') }}">back to list</a>

    <a href="{{ path('app_admin_bubble_edit', {'id': bubble.id}) }}">edit</a>

    {{ include('admin_bubble/_delete_form.html.twig') }}
{% endblock %}
