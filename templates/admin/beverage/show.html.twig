{% extends 'base.html.twig' %}

{% block title %}Beverage{% endblock %}

{% block body %}
    <h1>Beverage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ beverage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ beverage.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ beverage.price }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ beverage.image }}</td>
            </tr>
            <tr>
                <th>Is_new</th>
                <td>{{ beverage.isNew ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_beverage_index') }}">back to list</a>

    <a href="{{ path('app_admin_beverage_edit', {'id': beverage.id}) }}">edit</a>

    {{ include('admin_beverage/_delete_form.html.twig') }}
{% endblock %}
