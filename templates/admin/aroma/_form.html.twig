{{ form_start(form) }}
{{ form_widget(form) }}
<div class="d-flex justify-content-between mt-5 mb-2">
	<a href="{{ path('app_admin_aroma_index') }}" class="btn btn-outline-primary border-2 rounded-pill text-tertiary fw-bolder">Retour vers la liste</a>
	<button class="btn btn-primary rounded-pill text-tertiary fw-bolder">{{ button_label|default('Sauvegarder') }}</button>
</div>
{{ form_end(form) }}
